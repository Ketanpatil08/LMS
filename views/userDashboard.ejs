<!DOCTYPE html>
<html lang="en">
<head>
  <title>User Dashboard</title>
  <link rel="stylesheet" href="/styles/userDashboard.css">
  <link rel="stylesheet" href="/styles/adminSidebar.css">
  <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.4.2/css/all.min.css">
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/css/bootstrap.min.css" rel="stylesheet">
  <style>
    .d-flex { display: flex; min-height: 100vh; }
    .main-content { flex: 1; padding: 40px 40px 0 40px; background: #f8fafc; min-height: 100vh; }
  </style>
</head>
<body>
  <div class="d-flex">
    <%- include('userSidebar.ejs') %>
    <div class="main-content">
      <div class="dashboard-container">
        <div class="welcome-card">
          <i class="fa fa-user-circle"></i>
          <div>
            <div class="welcome-title">Welcome, <%= userName %>!</div>
            <div class="welcome-desc">Here's your library activity at a glance.</div>
          </div>
        </div>
        <div class="dashboard-cards">
          <div class="dashboard-card issued">
            <span class="dashboard-card-title"><i class="fa fa-cube"></i> Books Issued</span>
            <span class="dashboard-card-value"><%= issued %></span>
          </div>
          <div class="dashboard-card returned">
            <span class="dashboard-card-title"><i class="fa fa-check-square"></i> Books Returned</span>
            <span class="dashboard-card-value"><%= returned %></span>
          </div>
          <div class="dashboard-card overdue">
            <span class="dashboard-card-title"><i class="fa fa-clock"></i> Overdue</span>
            <span class="dashboard-card-value"><%= overdue %></span>
          </div>
        </div>
      </div>
    </div>
  </div>
</body>
</html>